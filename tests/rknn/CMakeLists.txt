# ------------------------------------------------------------
# name
# ------------------------------------------------------------
set(NAME test_rknn)

# ------------------------------------------------------------
# sources
# ------------------------------------------------------------
file(GLOB TEST_SRCS *.cpp)

# ------------------------------------------------------------
# build
# ------------------------------------------------------------
add_executable(${NAME} ${TEST_SRCS})

target_link_libraries(${NAME} PRIVATE
        common yaml-cpp
        maixos::vcodecx::rkffmpeg
        maixos::inferencex::yolo5
)
